---
import type { ColumnBlockObjectResponse } from "@notionhq/client/build/src/api-endpoints";
interface Props {
  block: ColumnBlockObjectResponse;
}

const { block } = Astro.props;
---

<div id={block.id} class=`notion-selectable notion-column-block`>
  <slot />
</div>
