{
  "name": "notro-ws",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "npm run dev --workspace=notro-tail",
    "build": "npm run build --workspace=notro-tail",
    "preview": "npm run preview --workspace=notro-tail"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "prettier-plugin-astro": "^0.13.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
